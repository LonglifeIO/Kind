# Rebuild in progress
Previous CLAUDE.md, skills, and hooks archived to _claude-archive/2026-07-30
for a zero-based rebuild.
- First real session here: run /init to regenerate project facts
  (commands, layout, architecture) from the codebase itself.
- Do NOT bulk-import from the archive. If /init misses a build/test
  command or hard environment constraint, copy that specific fact over.
  No behavioral rules ("always/never X") come back pre-emptively.
- A specific skill may be restored from the archive when a session
  actually needs it — individually, never wholesale.
- New behavioral lines only in response to observed failures (# shortcut),
  stated as the minimal rule that would have prevented that failure.
- Once /init has run and this file has real content, delete this block.
